{
  "name": "flickr-photowall",
  "version": "1.0.0",
  "description": "A webapplication that uses the FLICKR-public API to display photos",
  "main": "./server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "nodemon": "nodemon ./server/server.js"
  },
  "author": "Tomas Wandahl",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
